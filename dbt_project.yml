name: 'dbt_ai_poc'
version: '1.0.0'

profile: 'default'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  dbt_ai_poc:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
